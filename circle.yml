machine:
  node:
    version: 6.9.4

dependencies:
  override:
    - npm i
    - nvm install 8.4.0

test:
  override:
    - nvm use 6.9.4 && npm t && npm run lint
    - nvm use 8.4.0 && npm t && npm run lint

experimental:
  notify:
    branches:
      only:
        - master
